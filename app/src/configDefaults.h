#pragma once

#include "wx/wx.h"

class ConfigDefaults {
  public:
    const wxString ANIMATE_CONTRACTION_ID = "AnimateContraction";
    const wxString ANIMATE_ALIGNMENT_ID = "AnimateAlignment";

    const bool ANIMATE_CONTRACTION = true;
    const bool ANIMATE_ALIGNMENT = true;
};